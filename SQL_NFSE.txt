ALTER TABLE NOFISA
    ADD NUMERO_NFSE VARCHAR(20),
    ADD CODIGO_NFSE VARCHAR(10),
    ADD ARQ_XML_RPS VARCHAR(100),
    ADD ARQ_XML_NFSE VARCHAR(100),
    ADD XML_NFSE BLOB SUB_TYPE 1 SEGMENT SIZE 80,
    ADD XML_RPS BLOB SUB_TYPE 1 SEGMENT SIZE 80;

ALTER TABLE CONCESSIONARIA
ADD LOGO_PREFEITURA VARCHAR(100);